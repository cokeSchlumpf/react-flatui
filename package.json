{
  "name": "react-flatui",
  "version": "0.0.1",
  "description": "A UI Library based on React.",
  "repository": {
    "type": "git",
    "url": "git://github.com/cokeSchlumpf/react-flatui.git"
  },
  "main": "js/app.js",
  "stability": "experimental",
  
  "dependencies": {
    "keymirror": "~0.1.0",
    "object-assign": "^2.0.0",
    "react": "^0.13.0",
    "less": "~2.4.0"
  },
  
  "devDependencies": {
    "browserify": "~9.0.3",
    "envify": "~3.2.0",
    "jest-cli": "~0.4.0",
    "reactify": "~1.0.0",
    "react-tools": "~0.12.0",
    "uglify-js": "~2.4.15",
    "watchify": "^2.1.1",
    "catw": "~0.2.0"
  },
  
  "scripts": {
    "start": "watchify -o js/bundle.js -v -d .",
    "build-css": "catw -c 'lessc -' 'less/example.less' > css/bundle.css",
    "build-js": "NODE_ENV=production browserify . | uglifyjs -cm > js/bundle.min.js",
    "build": "npm run build-css && npm run build-js",
    "test": "jest"
  },

  "author": "Michael Wellner",

  "browserify": {
    "transform": [
      [ "reactify", { "harmony": true } ],
      "envify"
    ]
  },

  "jest": {
    "rootDir": "./js"
  }
}